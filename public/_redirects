/resources      https://resources.spottail.ai/        200
/resources/*    https://resources.spottail.ai/:splat  200

/sitemap.xml    /sitemap.xml    200

# NOTE: do not add trailing-slash redirect rules for /become-a-supplier/*.
# Netlify matches /path and /path/ as equivalent, so such a rule matches the
# non-slash URL too and redirects it to itself (ERR_TOO_MANY_REDIRECTS).
# The prerenderer writes flat <slug>.html files instead, and Netlify's own
# Pretty URLs handling serves the non-slash URL and 301s the slash variant.

/*    /index.html   200
